{
  "name": "dreamstream-studio-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that runs AI-built apps in containers and returns a live preview URL.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "~5.8.2",
    "wrangler": "^4.40.0"
  }
}
