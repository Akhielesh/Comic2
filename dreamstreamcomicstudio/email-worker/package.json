{
  "name": "dreamstream-email-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that sends branded transactional email via Cloudflare Email Sending, for the Railway backend (HMAC) and the Supabase auth hook.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "~5.8.2",
    "wrangler": "^4.40.0"
  }
}
